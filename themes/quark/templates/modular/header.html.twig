{% set grid_size = theme_var('grid-size') %}
{% set image = page.media.images|first %}

<section class="section modular-header {{ page.header.class}} bg-gray">
    <section class="container {{ grid_size }}">
        <div class="columns {{ page.header.image_align|default('align-right') }}">
            {% if image %}
            <div class="column col-4 col-md-12 v-center h-center">
                {{ image.html('', 'header picture', 's-circle p-2')|raw }}
            </div>
            <div class="column col-8 col-md-12 v-center">
                {{ content|raw }}
            </div>
            {% else %}
            <div class="column col-12 p-centered">
                {{ content|raw }}
            </div>
            {% endif %}
        </div>
    </section>
</section>